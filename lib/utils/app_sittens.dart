class AppSittens {
  AppSittens._();

  // default app settings (can be read/written with SharedPreferences)
  static const String defaultLanguage = 'en';
  static const String defaultTheme = 'light'; // 'light' or 'dark'
}
